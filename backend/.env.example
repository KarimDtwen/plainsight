# Copy to backend/.env for local development. Real values are NEVER committed;
# production values live in the Render dashboard (sync: false in render.yaml).

APP_ENV=development

# Supabase (M1+) — project URL + service-role key from the Supabase dashboard.
SUPABASE_URL=https://kwofqdccqejkxtvqjmue.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imt3b2ZxZGNjcWVqa3h0dnFqbXVlIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NDYzNDUyNywiZXhwIjoyMTAwMjEwNTI3fQ.tLG4mHvyUAqoklmTzyu0EYjMuIeYgwXo3AHKdgAe_ts

# Dashboard admin login (single-user) and JWT signing secret.
ADMIN_PASSWORD=YOUR_ADMIN_PASSWORD
JWT_SECRET=YOUR_LONG_RANDOM_SECRET
JWT_TTL_HOURS=168

# Comma-separated dashboard origins (production). Localhost is auto-allowed
# outside production.
ALLOWED_ORIGINS=
